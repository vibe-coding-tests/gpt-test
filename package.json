{
  "name": "pokekart",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "PokéKart — a Mario Kart-style browser racer with the original 151 Pokémon",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "three": "^0.160.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/three": "^0.160.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
